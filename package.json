{
  "name": "ohshrt",
  "version": "1.0.0",
  "private": true,
  "description": "Password-gated URL shortener on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
